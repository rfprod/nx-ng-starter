{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "client-store-router",
  "prefix": "app",
  "projectType": "library",
  "sourceRoot": "libs/client-store-router/src",
  "tags": ["scope:client-store-router", "type:data-access"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "eslintConfig": "{projectRoot}/eslint.config.mjs",
        "lintFilePatterns": ["{projectRoot}/**/*.ts", "{projectRoot}/**/*.html"]
      }
    },
    "prettier-check": {
      "executor": "./tools/executors/prettier:check",
      "options": {
        "config": ""
      }
    },
    "stylelint-check": {
      "executor": "./tools/executors/stylelint:check",
      "options": {
        "config": ""
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "options": {
        "compiler": "swc",
        "configFile": "libs/client-store-router/vitest.config.mts",
        "coverageProvider": "istanbul",
        "uiFramework": "angular"
      }
    },
    "tsc-check": {
      "executor": "./tools/executors/tsc:check",
      "options": {
        "tsConfig": "{projectRoot}/tsconfig.lib.json"
      }
    }
  }
}
