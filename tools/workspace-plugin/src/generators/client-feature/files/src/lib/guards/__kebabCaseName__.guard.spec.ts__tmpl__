import { TestBed } from '@angular/core/testing';

import { App<%= pascalCaseName %>Guard } from './<%= kebabCaseName %>.guard';

describe('App<%= pascalCaseName %>Guard', () => {
  let guard: App<%= pascalCaseName %>Guard;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    guard = TestBed.inject(App<%= pascalCaseName %>Guard);
  });

  it('should be created', () => {
    expect(guard).toBeTruthy();
  });
});
