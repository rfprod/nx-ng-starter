{
  "$id": "tsconfig-migrations",
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "properties": {
    "migrate": {
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "description": "The migration context.",
      "type": "string",
      "enum": ["vitest-types"]
    },
    "debug": {
      "description": "The instruction to print detailed program output in the terminal.",
      "type": "boolean"
    }
  },
  "required": ["migrate"],
  "type": "object"
}
