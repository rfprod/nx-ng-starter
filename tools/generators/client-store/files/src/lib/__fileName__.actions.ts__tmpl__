import { createAction } from '@ngrx/store';

import { featureName } from './<%= fileName %>.interface';

const type = (name: string) => `[${featureName}] ${name}`;

const action = createAction(type('action')); // TODO

export const <%= fileName %>Actions = {
  action,
};
