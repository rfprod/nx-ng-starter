import { actionType } from '@app/client-util-ngrx';
import { createAction } from '@ngrx/store';

import { featureName } from './<%= kebabCaseName %>.interface';

const action = createAction(actionType(featureName, 'action')); // TODO

export const <%= camelCaseName %>Actions = {
  action,
};
