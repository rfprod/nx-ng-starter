{
  "executors": {
    "check": {
      "implementation": "./check",
      "schema": "./schema.json",
      "description": "Check SCSS files for formatting errors using stylelint."
    },
    "configure": {
      "implementation": "./configure",
      "schema": "./schema.json",
      "description": "Configure the executor - add targets to the application and library configurations."
    }
  }
}
